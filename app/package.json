{
  "name": "imjustacube-app",
  "version": "1.0.0",
  "private": true,
  "description": "The native shell around I'm Just A Cube. The game itself is the repository root; tools/build-app.js copies it into www/.",
  "scripts": {
    "sync": "node ../tools/build-app.js && cap sync",
    "android": "node ../tools/build-app.js && cap sync android && cap open android",
    "ios": "node ../tools/build-app.js && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "7.2.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capgo/native-purchases": "7.19.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
